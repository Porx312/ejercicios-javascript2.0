

// Crear un objeto vacío.


// Crear un objeto con una propiedad específica.


// Agregar una propiedad a un objeto existente.


// Eliminar una propiedad de un objeto existente.


// Obtener el valor de una propiedad de un objeto existente.


// Actualizar el valor de una propiedad de un objeto existente.


// Comprobar si un objeto tiene una propiedad específica.


// Obtener una lista de todas las propiedades de un objeto.


// Obtener una lista de todas las propiedades de un objeto, incluyendo las propiedades heredadas.


// Obtener una lista de todas las propiedades de un objeto, excluyendo las propiedades heredadas.


// Comprobar si un objeto es vacío.


// Copiar un objeto.


// Fusionar dos objetos en uno solo.


// Crear un objeto con un método específico.


// Llamar a un método de un objeto.


// Llamar a un método de un objeto con argumentos.


// Obtener una lista de todos los métodos de un objeto.


// Obtener una lista de todos los métodos de un objeto, incluyendo los métodos heredados.


// Obtener una lista de todos los métodos de un objeto, excluyendo los métodos heredados.


// Obtener una lista de todas las propiedades y métodos de un objeto.


// Obtener una lista de todas las propiedades y métodos de un objeto, incluyendo las propiedades y métodos heredados.


// Obtener una lista de todas las propiedades y métodos de un objeto, excluyendo las propiedades y métodos heredados.


// Crear un objeto con un constructor.


// Crear un objeto con una función de fábrica.


// Comprobar si un objeto es una instancia de una clase específica.


// Heredar propiedades de un objeto padre a un objeto hijo.


// Crear una clase con propiedades y métodos.


// Crear una clase que herede de otra clase.


// Crear una instancia de una clase.


// Llamar a un método de una instancia de una clase.


// Agregar una propiedad a una clase.


// Agregar un método a una clase.


// Crear un objeto con una propiedad de solo lectura.


// Crear un objeto con una propiedad de solo escritura.


// Crear un objeto con una propiedad que se puede leer y escribir.


// Crear una clase con una propiedad de solo lectura.


// Crear una clase con una propiedad de solo escritura.


// Crear una clase con una propiedad que se puede leer y escribir.


// Crear un objeto con una propiedad de solo lectura y un método que la actualiza.


// Crear una clase con una propiedad de solo lectura y un método que la actualiza.


// Crear una clase con una propiedad de solo escritura y un método que la lee.


// Crear una clase con una propiedad que se puede leer y escribir y un método que la actualiza.


// Crear una clase con una propiedad que se puede leer y escribir y un método que la lee.


// Crear una clase con una propiedad que se puede leer y escribir y un método que la borra.


// Crear una clase con una propiedad que se puede leer y escribir y un método que la valida.


// Crear una clase con una propiedad


// Crear una clase con una propiedad que se puede leer y escribir y un método que la valida.


// Crear una clase con un método que devuelve una nueva instancia de la misma clase.


// Crear una clase con un método que devuelve una copia del objeto.


// Crear una clase con un método que devuelve un objeto literal con las propiedades y valores del objeto.


// Crear una clase con un método que devuelve un array con los valores de todas las propiedades del objeto.


// Crear una clase con un método que devuelve un array con los nombres de todas las propiedades del objeto.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las claves y valores invertidos.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con los valores transformados por una función específica.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero solo aquellas que cumplen una condición específica.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero excluyendo aquellas que cumplen una condición específica.


// Crear una clase con un método que devuelve la suma de todas las propiedades numéricas del objeto.


// Crear una clase con un método que devuelve la media de todas las propiedades numéricas del objeto.


// Crear una clase con un método que devuelve la propiedad con el valor más alto de todas las propiedades numéricas del objeto.


// Crear una clase con un método que devuelve la propiedad con el valor más bajo de todas las propiedades numéricas del objeto.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades numéricas redondeadas a un número específico de decimales.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades numéricas convertidas a una unidad específica.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades booleanas negadas.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades booleanas convertidas a números.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a mayúsculas.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a minúsculas.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string truncadas a una longitud específica.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string reemplazando un substring específico por otro.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string eliminando todos los espacios en blanco al inicio y al final.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string eliminando todos los espacios en blanco al inicio.


// Crear una clase con un método que devuelve un objeto con todas las propiedades


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string eliminando todos los espacios en blanco al final.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string eliminando todas las ocurrencias de un substring específico.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string truncadas a una longitud específica y con un sufijo agregado al final.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string truncadas a una longitud específica y con un prefijo agregado al inicio.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar un substring específico.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a camelCase.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a snake_case.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a kebab-case.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a Title Case.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar un substring específico y con un carácter específico como reemplazo.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar una expresión regular específica y con un carácter específico como reemplazo.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a un slug específico.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar un número específico de caracteres al inicio y al final.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar un número específico de caracteres al inicio.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar un número específico de caracteres al final.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar todos los caracteres excepto los últimos n caracteres.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string enmascaradas para ocultar todos los caracteres excepto los primeros n caracteres.


// Crear una clase con un método que devuelve un objeto con todas las propiedades del objeto original, pero con las propiedades string convertidas a una representación de base64.



// Crear una clase con un método que recibe dos objetos y devuelve un objeto con todas las propiedades que se encuentran en ambos objetos, con los valores del segundo objeto sobrescribiendo los valores del primer objeto en caso de existir una propiedad en ambos objetos.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores numéricos, reemplazando los valores por su valor absoluto.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores booleanos, reemplazando los valores por su valor inverso (true por false y viceversa).



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores string, reemplazando los valores por su valor en mayúsculas.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores string, reemplazando los valores por su valor en minúsculas.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores string, reemplazando los valores por su valor con la primera letra en mayúscula.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores string, reemplazando los valores por su valor con la primera letra en minúscula.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores string, reemplazando los valores por su valor con la primera letra de cada palabra en mayúscula (Title Case).



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores numéricos, redondeando los valores al número entero más cercano.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores numéricos, convirtiendo los valores a su equivalente en dólares con dos decimales.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores string, reemplazando los valores por su valor en formato de fecha, según un formato específico.



// Crear una clase con un método que recibe un objeto y devuelve un objeto con todas las propiedades que tienen valores string, reemplazando los valores por su valor en formato de hora, según un formato específico.